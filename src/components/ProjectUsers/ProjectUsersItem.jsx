import React, { useState } from "react";

function ProjectsItem({ dispatch, project }) {

  return (
    <div className='projectUsers'>
      <div className='projectUser__name'>{projectUser.name}</div>
    </div>
  );
}

export default ProjectsItem;
